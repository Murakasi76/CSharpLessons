🧠 CSharpLessons

This solution contains a collection of focused C# projects, each dedicated to a specific learning topic. Every project is self-contained and designed to reinforce core programming concepts, best practices, and real-world patterns.

📁 Project List

\- LessonArray — Working with arrays, user input, and memory allocation



📘 LessonArray

Topic: Array fundamentals and dynamic resizing

This project demonstrates basic array operations in C#:

\- Prompts the user to enter the desired array length

\- Fills the array using a custom formula: (i + i) \* i / 3

\- Creates a new array with 5 additional slots

\- Copies the original array into the new one

\- Prints all elements of the new array to the console

🔍 Key Concepts

\- Console.ReadLine input parsing with validation

\- Array initialization and indexing

\- CopyTo method for array duplication

\- foreach loop for output

▶️ How to Run

\- Open the solution CSharpLessons in Visual Studio

\- Set LessonArray as the startup project

\- Run the application (Ctrl + F5)

\- Enter a number when prompted (e.g., 10)

\- View the generated array output in the console



